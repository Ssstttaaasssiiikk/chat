import 'package:flutter/material.dart';

class Pagescreen extends StatelessWidget {
  const Pagescreen({super.key});


  @override
  Widget build(BuildContext context) {
    return const Text('авторизован');
  }
}
